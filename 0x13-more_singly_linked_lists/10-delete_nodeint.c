#include <stdilib.h>
#include <stdio.h>
#include <tring.h>
#include "lists.h"

/**
 * nodeint_at_index - deletes the node at index index of a listint_t linked list.
 * @head: had of the node
 * @index: 
 * Returns: 1 if it succeeded, -1 if it failed
 int delete_nodeint_at_index(listint_t **head, unsigned int index)
