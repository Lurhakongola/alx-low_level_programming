include "mainh"
/**
 * _memset - fills memory with a constant byte
 * @s: memory area to be filled
 * @b: char to copy
 * @n: niumber of times to copy b
 *
 */

char *_memset(char *s, char b, unsigned int n);
{
	unsigned int i;

	for(i=o; i<n; i++)
	{
		s[i] = b;
	}
	return (s)
}
